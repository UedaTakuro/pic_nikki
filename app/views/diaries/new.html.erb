<%= form_with model: @diary, local:true do |f| %>
  
  <%= f.file_field :image, placeholder: "画像", accept: "image/*" %>
  
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create' %>
<% end %>